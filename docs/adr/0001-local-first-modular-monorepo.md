# Local-first modular monorepo

The repository keeps reusable Python ML modules, a FastAPI adapter, and a React/Vite frontend in one repository while preserving independent local workflows. Docker and MLflow remain optional because mandatory infrastructure would obscure the portfolio's core ML and API behavior and make development less reliable.
